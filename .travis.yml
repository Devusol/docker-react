sudo: required
services: 
  - docker

before_install:
  - docker build -t docker.devusol.net/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI-true docker.devusol.net/docker-react npm run test